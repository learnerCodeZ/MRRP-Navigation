# HL2 开发迭代方式：原生部署 vs 全息远程（Holographic Remoting）

> 日期：2026-07-18（2026-07-19 更新：补充 Holographic Remoting 实际配置步骤 + 参考链接）
> 问题：每改一次 HL2（HoloLens2）应用，是不是都要重新部署一次？
> 结论：**原生部署是**；但开发期用 **Holographic Remoting（全息远程）** 可以免重部署、秒级迭代。

---

## 0. 一句话

改代码/Prefab 后想看效果，**原生部署**每次都要 rebuild + redeploy（分钟级，慢）；但 **Holographic Remoting** 让 Unity Editor 直接 Play → Wi-Fi 串流到 HL2，**改完再 Play 即时看**，且手势/头部追踪从 HL2 真机回传——**不用重新部署**，秒级迭代。

---

## 1. 两种方式对比

| 维度 | 原生部署（Build + Deploy） | Holographic Remoting（全息远程） |
|---|---|---|
| 每次改动后 | **rebuild（IL2CPP，几分钟）+ redeploy** | **再点一次 Play**，秒级 |
| HL2 上跑什么 | 独立 AppX 应用（**脱机**，演示用） | Remoting Player（**依赖 PC**，开发用） |
| 手势/头部追踪 | 真机原生 | 真机→回传 PC（**真手势**，非模拟） |
| 网络 | 不需要（脱机） | 需 PC + HL2 **同一 Wi-Fi** |
| 延迟 | 无 | 微小（局域网，体验接近真机） |
| 何时用 | 第一次验 build 链路 / **最终交付演示** | **开发期改 UI / 调功能**（主力） |

---

## 2. 原生部署：每次改都要 rebuild + redeploy

- 流程：Unity Build → 生成 UWP `.sln` → VS2022 部署（USB/WiFi/Device Portal）到 HL2。
- 改了**任何**代码/Prefab/资源 → 都得重新 Build（IL2CPP 编译几分钟）+ 重新 Deploy。
- 做完整 UI 美化（多次微调）如果全走这条路，每次几分钟，很痛。
- 优化：首构建建后 IL2CPP 增量编译快些；WiFi/Device Portal 部署比 USB 方便一点——但仍要重 build。

---

## 3. Holographic Remoting（开发神器，免重部署）

- **PC 上 Unity Editor 点 Play**，画面 + 音频**实时串流到 HL2**；HL2 的**手势、头部追踪、空间映射回传到 PC**。
- 改完代码/Prefab → **再点一次 Play** → 头显里立刻看新效果，**不 Build、不 Deploy**。
- 用的是**真手势**（HL2 回传），不是 Editor 鼠标模拟——所以手势画线逻辑能真实验证。

> 代价：PC + HL2 同一 Wi-Fi；HL2 跑 Remoting Player（**非脱机**，依赖 PC）；有微小延迟。**定位是开发调试，不是最终交付。**

---

## 4. 推荐迭代策略

```
① 先做一次原生 Build + Deploy —— 验证整条 build/deploy 链路能通（一次性痛苦）
        ↓
② 开发期改 UI / 调功能 —— 全用 Holographic Remoting（Editor Play 串流，秒级迭代）
        ↓
③ 定稿 —— 最后一次原生 Build + Deploy（脱机交付，演示用）
```

要点：
- **原生部署只做两次**：第一次验链路、最后交付。中间**全走 Remoting**。
- **UI 想改多少次都行**——Remoting 兜底，每次改完 Play 就在头显验证。
- 先验设备链路（最大未知），再用 Remoting 快速迭代（已知可行）。

---

## 5. Holographic Remoting 实际配置步骤（MRTK2 + OpenXR，2026-07-19 实测）

### HL2 端：

1. HL2 微软商店装 **Holographic Remoting Player**（免费）
2. 打开它 → 屏幕显示 HL2 的 **IP 地址**（如 `192.168.123.45`）和端口（默认 `8265`）
3. 保持 Remoting Player 打开状态

### Unity 端（XR Plug-in Management 配置）：

4. **Edit → Project Settings → XR Plug-in Management**
5. 选择 **PC 标签**（电脑图标，不是 UWP 标签）
6. 勾选 **OpenXR**（如果没有就先安装 `com.unity.xr.openxr` 包）
7. OpenXR 下勾选：
   - ✅ **Windows Mixed Reality feature group**
   - ✅ **Holographic Remoting remote app feature group**
8. 展开 OpenXR 子页：
   - **Interaction Profiles** 里添加 **Microsoft Hand Interaction Profile**
   - **OpenXR Feature Groups** 里勾选：
     - ✅ **Holographic Remoting for Play Mode**（关键！不勾这个 Play 不会串流）
     - ✅ **Hand Interaction Poses**
     - ✅ **Hand Tracking**
     - ✅ **Mixed Reality Features**
   - Render Mode 设为 **Single Pass Instanced**
   - Play Mode OpenXR Runtime 设为 **System Default**

### Unity 端（连接 HL2）：

9. 菜单 → **Mixed Reality → Remoting → Holographic Remoting for Play Mode**
10. 弹出窗口里填入：
    - **Remote Host Name**：HL2 显示的 IP（如 `192.168.123.45`）
    - **Remote Host Port**：`8265`
11. 点 Unity 顶部 **Play** 按钮 → 自动连接 HL2，画面串流到头显

### 验证成功标志：

- HL2 屏幕从 Remoting Player 界面切换为 Unity 场景
- HL2 上能看到手部坐标系（手部追踪工作）
- Unity Editor Game 视图同步显示 HL2 看到的画面
- Console 无报错

### 常见问题：

- **连不上**：确认 PC 和 HL2 在同一 WiFi；检查 HL2 的 IP 是否正确；确认 HL2 防火墙放行 8265 端口
- **Play 后 HL2 黑屏**：确认 `Holographic Remoting for Play Mode` 已勾选（OpenXR Feature Groups 里）
- **手势不工作**：确认 Microsoft Hand Interaction Profile 已添加；确认 Hand Tracking 已勾选
- **有黄色⚠️警告**：通常是 Interaction Profile 缺少对应控制器绑定，不影响基本功能，可忽略
- **Library 文件夹损坏**：删除 `Library` 文件夹，Unity 会自动重建（需重新导入，几分钟）

> **注意**：XR Plug-in Management 里选 **PC 标签**（不是 UWP）。Holographic Remoting 是从 PC Editor 串流到 HL2，走的是 PC 的 XR 配置。

---

## 6. 何时用哪种（速查）

| 场景 | 用哪种 |
|---|---|
| 第一次验证 build/deploy 能不能通 | **原生部署** |
| 改 UI / 调手势 / 调功能（频繁迭代） | **Remoting** |
| 调 ROS-TCP 连接（HL2 连机器人） | Remoting 也行（HL2 经 PC 联网）；最终原生验证 |
| 给别人演示 / 论文录屏 / 脱机跑 | **原生部署**（脱机，不依赖 PC） |
| 出门前最终版 | **原生部署** |

---

## 7. 复盘要点

- **原生部署 = 每改必重 build + 重 deploy**（慢），是最终交付手段，不是日常迭代工具。
- **Holographic Remoting = Editor Play 串流到 HL2，改完即看、不用重部署，且真手势回传**——开发期主力。
- **策略：原生验链路一次 → Remoting 快速迭代 → 原生交付一次**。原生只做两次。
- Remoting 的限制：要 PC + 同 Wi-Fi、HL2 跑 Player（非脱机）、微延迟——所以是开发用，演示仍需原生部署。

---

**一句话**：每次改 HL2 应用，原生部署确实要重 build+重部署；但开发期用 Holographic Remoting（Editor Play 串流）可免重部署、秒级看效果、用真手势——所以原生只在"验链路"和"交付"各做一次，中间全走 Remoting。

---

## 8. 参考资料

### Microsoft 官方文档

- [Holographic Remoting 入门教程](https://learn.microsoft.com/zh-cn/training/modules/pc-holographic-remoting-tutorials/1-introduction) — 微软官方 Remoting 入门模块
- [使用 PC 资源开发 HL2 应用](https://learn.microsoft.com/zh-cn/windows/mixed-reality/develop/unity/use-pc-resources) — PC 开发模式概述
- [Holographic Remoting Player](https://learn.microsoft.com/en-us/windows/mixed-reality/develop/native/holographic-remoting-player) — Remoting Player 官方文档
- [预览和调试你的应用](https://learn.microsoft.com/zh-cn/windows/mixed-reality/develop/unity/preview-and-debug-your-app) — Unity Editor 调试方法
- [MRTK3 流式传输测试与部署](https://learn.microsoft.com/zh-cn/windows/mixed-reality/mrtk-unity/mrtk3-overview/test-and-deploy/streaming) — MRTK3 Remoting 配置
- [使用 Visual Studio 部署 HL2](https://learn.microsoft.com/zh-cn/windows/mixed-reality/develop/advanced-concepts/using-visual-studio?tabs=hl2) — 原生部署详细步骤
- [Windows Device Portal](https://learn.microsoft.com/zh-cn/windows/mixed-reality/develop/advanced-concepts/using-the-windows-device-portal) — HL2 设备信息门户（安装/调试/监控）
- [HoloLens 2 开发入门教程（8模块）](https://learn.microsoft.com/en-us/training/paths/beginner-hololens-2-tutorials/) — 微软官方HL2开发课程，含MRTK手部交互 + Holographic Remoting
